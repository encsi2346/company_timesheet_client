beforeEach(() => {
    cy.login()
})

describe('Create users', () => {
    it('Should create an employee', () => {
        //cy.insertUser('Példa','Gábor' többi paraméter)
    })
})

afterEach(() => {
    //cy.logout()
})